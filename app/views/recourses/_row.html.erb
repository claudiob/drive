<%# locals: (recourse: nil, heading: false) -%>
<% resource_columns.each do |name| -%>
  <%= column header: resource_column_title(name),
             value: (resource_cell(recourse, name) unless heading),
             heading: heading %>
<% end -%>
