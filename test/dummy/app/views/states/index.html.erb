<% content_for :title, 'States' %>

<ul class='list-group' data-view='host'>
  <% @resources.each do |state| %>
    <li class='list-group-item'><%= state.code %> &mdash; <%= state.name %></li>
  <% end %>
</ul>
